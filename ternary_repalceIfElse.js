//Wap to replace if by ternary 


const entry_status = true;

//if 
if(entry_status===true){
    console.log("so the entry status  of chiwiwi today is " + entry_status);
}

// ternary for the same if condition as above
 
const result = entry_status ? true : false;
console.log("so the entry status  of chiwiwi today is " + result);